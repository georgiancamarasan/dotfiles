- name: check if cargo is installed
  shell: command -v $HOME/.cargo/bin/cargo
  register: cargo_exists
  ignore_errors: true
  tags:
    - rust

- name: Update rustup
  shell: rustup update
  when: cargo_exists is succeeded

- name: Download Installer
  when: cargo_exists is failed
  get_url:
    url: https://sh.rustup.rs
    dest: /tmp/sh.rustup.rs
    mode: "0755"
    force: true
  tags:
    - rust

- name: install rust/cargo
  when: cargo_exists is failed
  shell: /tmp/sh.rustup.rs -y
  tags:
    - rust

- name: install bat
  shell: $HOME/.cargo/bin/cargo install bat
  tags:
    - rust
- name: install cargo-update
  shell: $HOME/.cargo/bin/cargo install --locked cargo-update
  tags:
    - rust
- name: install exa
  shell: $HOME/.cargo/bin/cargo install --locked exa
  tags:
    - rust
- name: install diskonaut
  shell: $HOME/.cargo/bin/cargo install --locked diskonaut
  tags:
    - rust
- name: install fd-find
  shell: $HOME/.cargo/bin/cargo install --locked fd-find
  tags:
    - rust
- name: install just
  shell: $HOME/.cargo/bin/cargo install --locked just
  tags:
    - rust
- name: install ripgrep
  shell: $HOME/.cargo/bin/cargo install --locked ripgrep
  tags:
    - rust
- name: install rm-improved
  shell: $HOME/.cargo/bin/cargo install --locked rm-improved
  tags:
    - rust
- name: install zoxide
  shell: $HOME/.cargo/bin/cargo install --locked zoxide
  tags:
    - rust
- name: install prettier
  shell: $HOME/.cargo/bin/cargo install --locked prettier
  tags:
    - rust
- name: install stylua
  shell: $HOME/.cargo/bin/cargo install --locked stylua
  tags:
    - rust
- name: install lsd
  shell: $HOME/.cargo/bin/cargo install --locked lsd
  tags:
    - rust
- name: install pueue
  shell: $HOME/.cargo/bin/cargo install --locked pueue
  tags:
    - rust
- name: install watchexec
  shell: $HOME/.cargo/bin/cargo install --locked watchexec-cli
  tags:
    - rust
- name: install bottom
  shell: $HOME/.cargo/bin/cargo install --locked bottom
  tags:
    - rust
- name: install paru
  when: ansible_distribution == 'Archlinux'
  shell: $HOME/.cargo/bin/cargo install paru --git https://github.com/Morganamilo/paru/
  tags:
    - rust
    - paru
- name: install tre
  shell: $HOME/.cargo/bin/cargo install tre-command
  tags:
    - rust
- name: install starship
  shell: $HOME/.cargo/bin/cargo install --locked starship
  tags:
    - rust
    - starship
#   shell: $HOME/.cargo/bin/cargo install lsp-ai
#   tags:
#     - rust
#     - llm
- name: install carl
  shell: $HOME/.cargo/bin/cargo install --locked carl
  tags:
    - rust
    - carl
- name: install tui-journal
  shell: $HOME/.cargo/bin/cargo install --locked tui-journal
  tags:
    - rust
    - tui-journal
